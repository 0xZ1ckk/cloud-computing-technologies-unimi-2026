server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ticketing
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

app:
  instance-id: ${INSTANCE_ID:ticket-1}
  notification-url: http://localhost:8083/notifications
